# Reference-only fitting contract.  The runtime reads configs/default.yaml;
# train.py selects these components explicitly.
task: multi_label_classification
components:
  - multilabel-selector-ranker
  - multilabel-probability-models
  - multilabel-residual-head
fixed_method_contract:
  hidden_dim: 128
  event_count: 40
  vocab_chunk_size: 16
  scoring_row_batch_size: 4
  candidate_cardinalities: [1, 2, 3, 4]
