model:
  name: google/medgemma-1.5-4b-it
  image_size: 896
  feature_batch_size: 8
  scoring_row_batch_size: 4
  token_chunk: 16
  max_new_tokens: 256
  seed: 0

multilabel:
  candidate_change_threshold: 0.05
  max_replacement_candidates: 32

paths:
  checkpoint_dir: checkpoints

